// exemplo 6 (arrays)
const randomNumber = Math.floor(Math.random() * 3);

const array = ["rock", "paper", "scissons"];
const result = array[randomNumber];

console.log(randomNumber);
console.log(result);