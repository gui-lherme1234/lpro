// ememplo 4

//exemplo com 2 cenários possíveis
let result = "";
if (n % 2 === 0) result === 0 ? "é par" : "é ímpar";
console.log(result);

// exemplo com 3 cenários possíveis
const randomNumber = Math.floor(Math.random() * 3);
result = 
    randomNumber === 0 ? "rock" : randomNumber === 1 ? "paper" : "scissors";
console.log(result);